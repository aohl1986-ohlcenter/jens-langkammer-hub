RewriteEngine On

# Fallback: rewrite pretty URL (e.g. /impressum) to HTML file (e.g. /impressum.html) internally
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.*)$ $1.html [L]
